<h1>Songs</h1>
<% @songs.each do |song| %>
<ul><strong>Name: <%= song.name %></strong></ul>
<ul>Artist: <%= @artists.find(song.artist_id).name %></ul>
<%= link_to "Edit", edit_song_path(song) %>
<%= link_to 'Delete', song, method: :delete, data: {confirm: 'Are you sure you?' } %>
<% end %>
