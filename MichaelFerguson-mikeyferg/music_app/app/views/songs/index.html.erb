<h1>Songs</h1>
<% @songs.each do |song| %>
<p><strong>Name: <%= song.name %></strong></p>
<ul>Artist: <%= @artists.find(song.artist_id).name %></ul>
<ul><%= link_to "Edit", edit_song_path(song) %>
<%= link_to 'Delete', song, method: :delete, data: {confirm: 'Are you sure you?' } %></ul>
<% end %>
