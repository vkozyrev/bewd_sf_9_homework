<h1><%= @movie.name %></h1>
<p><%= image_tag(@movie.movie_image, size: "100x100",)%></p>
<p><strong>Year Released: </strong><%= @movie.date_released %></p>
<p><strong>Description: </strong><%= @movie.description %></p>


<%= link_to "Edit", edit_movie_path(@movie) %>

<%= link_to "Back", movies_path %>

<h3>Reviews</h3>
<%= render partial: 'movies/review', collection: @movie.reviews %>

<%= link_to "Add a new Review", new_review_path(:movie_id => @movie.id) %>
