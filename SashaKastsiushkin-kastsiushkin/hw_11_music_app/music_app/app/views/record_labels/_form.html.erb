<%= simple_form_for @record_label, :html => {:class => "col-md-6 col-md-offset-3"} do |f| %>
  <%= f.input :name %>
  <%= f.input :image_url %>
  <%= f.submit "#{button_text}", :class => 'btn btn-primary btn-block' %>
<% end %>
