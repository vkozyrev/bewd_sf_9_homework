<nav class="navbar navbar-inverse">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#collapse-navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Home", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="collapse-navbar">
      <ul class="nav navbar-nav">
        <li> <%= link_to "Artists", artists_path %> </li>
        <li> <%= link_to "Songs", songs_path %></li>
        <li> <%= link_to "Labels", record_labels_path %></li>
      </ul>
      <!-- Search Form -->
      <%= form_tag(artists_path, :method => "get", id: "search-form", class: "navbar-form navbar-right") do %>
        <div class="input-group">
          <%= text_field_tag :search, params[:search], placeholder: "Search Users", class: "form-control" %>
          <span class="input-group-btn">
            <%= submit_tag "Search", class: "btn btn-default" %>
          </span>
        </div>
      <% end %>
      <!-- <%= simple_form_for artists_path(:search => params[:search]), :method => "get", html: { class: "navbar-form navbar-right" } do |f| %>
        <%= f.input :search, wrapper: :horizontal_input_group, label: false, placeholder: "Search" do %>
          <%= f.input_field :search, label: false, class: "form-control" %>
          <span class="input-group-btn">
            <%= f.submit "Search", :class => 'btn btn-default' %>
          </span>
        <% end %>
      <% end %> -->
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
