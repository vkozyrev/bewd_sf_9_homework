<%= simple_form_for @song, :html => {:class => "col-md-6 col-md-offset-3"} do |f| %>
  <%= f.input :name %>
  <%= f.association :artist, method: :artist_id %>
  <%= f.submit "Save", :class => 'btn btn-primary btn-block' %>
<% end %>
